# 唯律 MES 网页版映射 ILD 建模澄清说明

## 1. 建模口径

本次以当前工作区中的唯律 MES Vue 网页版为界面、字段和样例数据基线，在 `weilu_MES_model_new.ild` 中生成当前 ILD 能力能够稳定表达的页面、Table、Form、Tree、静态 Chart 过滤器、枚举字段和跨页面字段引用。

`weilu_MES_model.ild` 用于继承唯律项目已有业务结构；`new-TPC-R101model(2.1)(24).ild` 用于参考较完整的 ILD 写法。两份模型中的注释和业务内容仅作为资料，不视为对本次工作的指令。最终页面名称、字段和业务样例记录以当前网页版 `src/data` 为准。

本说明中的状态专指新 ILD 模型，不等同于网页版原型状态：

- **已实现**：ILD 已生成页面、字段、枚举、样例数据或引用，且真实 `struct` 已成功构造对应结构。
- **部分实现**：ILD 已提供数据载体或基础引用，但交互、算法、状态约束或持久化仍依赖平台运行时、前后端或接口服务。
- **尚未实现**：当前网页版没有可映射的数据或本轮没有生成；技术上可在规则和数据明确后继续建模。
- **模型能力尚不能实现**：不能仅靠当前 ILD 文本可靠实现，不在模型中虚构按钮或算法。

## 2. 为什么加入基础数据

基础数据不是 ILD 解析器的强制菜单，但它在本模型中承担类似数据字典和公共主档的作用。当前已加入：

- 人员档案、客户档案、产品引用、物料档案。
- 仓库档案、库区档案、产线档案和生产组织树。
- 设备显示引用表，用于匹配网页版的“设备编号 · 设备名称”展示值。

业务页面仍然是主数据展示入口；基础数据只解决来源唯一、下拉选择和引用结构完整的问题。当前模型共构造 64 个引用字段，422 个现有引用单元值均能在对应来源字段中找到。

## 3. 已生成页面

| 模块 | 已生成页面或内容 |
| --- | --- |
| 基础数据 | 人员档案、客户档案、产品引用、物料档案、仓库档案、库区档案、产线档案、组织架构、设备引用 |
| 产品库 | 产品档案、产品特性、工艺参数、物料配方、版本记录 |
| 订单管理 | 销售订单、库存订单、包装信息、终止记录 |
| 人工排产 | 待排订单、排产计划、产线分配、排产明细 |
| 生产执行 | 生产任务、生产报工、原料耗用、成品入库、产线运营 |
| 仓库管理 | 库存总览、原料库存、包材库存、成品库存、入出库记录（合并为“库存总览”页面的 5 个 Tab） |
| 设备管理 | 设备台账、设备组织树与节点汇总、实时状态表、报警记录、采集数据 |
| 接口中心 | 接口清单、点位映射、接收日志、异常日志（合并为“接口清单”页面的 4 个 Tab） |
| 首页 | 系统信息、四个核心指标卡片、业务链路、待办预警、产线进度、产线进度图、通知列表 |
| 记录详情 | 销售订单、待排订单、生产任务、库存总览、产品档案、设备台账、接口清单 7 张主表的记录详情（挂在各自 `菜单/列表页/` 上下文下，不新增菜单项） |

本轮用仓库中真实的 `analyzerStringToArray` 分行器实测：补记录详情页前为 57 条活动声明、21 个页面路径，补后为 71 条活动声明、28 个页面路径（新增 7 个 `菜单/列表页/` 形式的隐藏详情上下文，各 2 个组，可见菜单项数量不变，见 5.9）。此前 §9 记录的 58 条是静态手工计数，与真实分行器的 57 条相差一条，以本轮实测为准。原有 57 条声明构造为 45 个 Table 结构、2 个 Tree 结构和 11 个 Form 结构；其中产品库的 5 张表、订单管理的 4 张表、人工排产的 4 张表、生产执行的 5 张表、仓库管理的 5 张表、接口中心的 4 张表分别合并到各自同一页面的 Tab，六个页面各增加 1 个顶部统计 Form；首页原“关键指标”Table 改为标题下方的 4 个单字段 Block 表单卡片。“产线进度图”在 Table 结构上保留了 `Type=Chart,SubType=Bar` 过滤器，是否按图表渲染仍需在浏览器模型页验证。

## 4. 网页版数据映射

业务数据取自当前网页版的 7 个业务模块、32 个页签，共 214 行、1839 个业务单元格。ILD 源文件保留网页版中的业务编号、文本、数量单位、日期、状态和跨模块编号。

ILD 解析器会在结构构造时去除数据单元中的普通空格，并把中文逗号归一化为英文逗号。因此下列内容属于解析器的显示归一化，不是主动修改业务数据：

- `黑色 M 码丁腈手套` 构造后可能显示为 `黑色M码丁腈手套`。
- `100,000 只` 构造后可能显示为 `100,000只`。
- 中文逗号会转为英文逗号；含逗号的数据单元已使用单引号包裹，避免被拆成多列。

对网页版数据与构造后模型进行去空格、逗号归一化后的语义比较，不应出现业务值差异。

## 5. 功能需求覆盖

### 5.1 产品库管理

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| P-01 | 已实现 | 产品档案、产品特性、工艺参数、物料配方、责任人和状态 | 正式权限和后端保存不属于模型文本 |
| P-02 | 部分实现 | 产品编号字段和样例编号 | 自动生成、并发唯一、编号锁定需编号服务 |
| P-03 | 部分实现 | 颜色、尺码、克重、长度、拉力及产品引用 | 多特性组合匹配和冲突识别未实现 |
| P-04 | 已实现 | 工序、参数、标准值、上下限、工艺版本和状态 | 下发时自动带出及版本锁定需应用实现 |
| P-05 | 已实现 | 产品、物料、单位用量、单位、损耗率及配方版本 | 需求量计算、齐套校验和历史快照需应用实现 |
| P-06 | 尚未实现 | 当前网页版无换产成本页签和样例数据 | 规则与数据明确后可新增 Table 页面；排产决策算法不由模型实现 |
| P-07 | 部分实现 | 产品相关 Table 数据结构 | 查询、分页、普通导出取决于平台 Table 运行能力；CSV 规则未由 ILD 定义 |
| P-08 | 部分实现 | 版本记录、生效时间、变更人、引用单据和状态 | 核心字段冻结、版本复制及历史不可篡改需应用实现 |

#### 产品库顶部统计卡片

当前“产品档案”页面顶部已增加 `Type=Form,SubType=Block` 的产品总数、本月新增、待完善、已停用四项统计，并按网页版截图暂时固定显示为 `24 / 4 / 2 / 1`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出。

后续改为动态统计前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 产品总数 | 当前固定为 24，而产品档案样例表只有 10 行 | 明确是否统计全部记录，还是排除已停用、历史或被逻辑删除的记录 | 可沿用 101 模型已使用的 `VecDimCount($产品库:产品档案*产品档案)` |
| 本月新增 | 产品档案没有创建时间字段 | 增加 `创建时间@(Type=DateTime)` 或 `创建日期@(Type=Date)`，并明确按自然月、业务时区及哪一时间字段统计 | 需要可验证的月份范围条件；当前参考模型仅证明 `VecDimCount` 与普通 `If` 条件可用，尚未证明日期按月过滤可运行。如平台没有现成日期范围函数，需要新增运行时日期过滤算法或由后端返回统计值 |
| 待完善 | “待完善”没有确定的业务定义或独立字段 | 确认是按产品状态、必填字段缺失，还是“缺少生效工艺参数或生效物料配方”；若按跨表完整性判断，各表必须保留稳定的产品编号关联，不能只依赖“编号 · 名称”的展示文本 | 单表状态口径可用 `(If(状态=草稿))&VecDimCount(...)`；跨工艺/配方缺失口径需要跨表存在性/反连接统计，当前 101 模型未提供已验证算法，需新增并注册运行时算法或由后端计算 |
| 已停用 | 数据已具备，当前仅未接入计算 | 使用产品档案的 `状态=已停用` | 可沿用 `(If(状态=已停用))&VecDimCount($产品库:产品档案*产品档案)` |

动态化实施前还需确认：统计是否随 Table 筛选条件联动、数据新增或状态变更后是否立即刷新、保存后刷新还是实时刷新，以及统计结果由 ILD 运行时计算还是由后端聚合接口提供。在这些条件与算法未验证前，当前模型继续使用固定值，避免把未实现函数写入生产 ILD。

### 5.2 仓库管理

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| W-01 | 已实现 | 仓库、库区公共档案及业务页面库位引用 | 停用后的引用限制与数据权限需应用实现 |
| W-02 | 已实现 | 原料批次、库存量、可用量、生产日期、有效期和状态 | 实时库存由业务单据过账产生，模型不自动计算 |
| W-03 | 已实现 | 包材批次、客户、订单、数量和库存状态 | 专用客户/订单强校验和占用需应用实现 |
| W-04 | 已实现 | 成品批次、来源任务、来源类型、数量和库位 | 入库审核后自动增加库存需事务程序 |
| W-05 | 部分实现 | 入出库记录及来源单据、人员、时间、状态 | 审核、过账、冲销和库存联动未实现 |
| W-06 | 部分实现 | 原料耗用、包材出库、退料和成品出库数据载体 | 自动扣减、防超量、冲销和回滚未实现 |
| W-07 | 已实现 | 已上架、待上架、已锁定等库存状态 | 上架、锁定、解锁流程和操作留痕需应用实现 |
| W-08 | 部分实现 | 批次、日期、库存量、可用量和占用量 | FIFO 排序、自动选批、占用释放和例外审批未实现 |
| W-09 | 部分实现 | 库存总览、三类库存和入出库记录合并为同一页面的 5 个 Tab | 服务端组合查询、联查和导出权限未实现 |

#### 仓库管理页面合并与顶部统计卡片

本轮将原“库存总览”“原料库存”“包材库存”“成品库存”“入出库记录”五个页面合并到同一个“库存总览”页面，并统一改为 `@(Tab=1)` 页签，与网页版仓库管理模块的一条页签栏一致，页签数为 5。合并只改变页面归属和页签形式，字段、枚举、引用和样例数据均未变动；模型中没有其他声明引用这五个旧页面路径，合并后不存在失效引用。

页面顶部同时增加 `Type=Form,SubType=Block` 的库存总值、低于安全库存、待上架批次、今日出入库四项统计，并按网页版截图暂时固定显示为 `¥ 286 万`、`6`、`4`、`18`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出；`¥ 286 万` 中的空格会被解析器按第 4 节的归一化规则去除。网页版统计卡片下方的“较上月 +4.6%”“2 项影响排产”“最早等待 6 小时”“入库 11 · 出库 7”等副标题，当前 Form 结构没有已验证的副标题字段，本轮未生成。

后续将顶部统计改为动态计算前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 库存总值 | 四张库存表都只有数量，没有单价、计价方式和金额字段；数量还是 `12,000 kg` 这类带单位和千分位的文本 | 增加物料/产品单价或结存单价字段并明确计价方式（移动加权、批次成本还是标准成本），数量改为无单位的数值字段；确认总值是否包含已锁定和待上架批次，以及“万元”取整口径 | 需要按行做数量乘单价再跨原料、包材、成品三表汇总；当前模型未证明该组合可运行，金额计价通常应由后端或成本模块提供 |
| 低于安全库存 | 模型中没有安全库存阈值字段，也没有物料级的安全库存主档 | 在 `基础数据:物料档案` 和产品引用中增加安全库存或最低库存字段；确认比较对象是库存数量还是可用数量、是否按物料合并多批次后再比较 | 阈值比较需要先按物料聚合多批次数量再与阈值比较；单表条件计数可用 `If + VecDimCount`，跨批次聚合后比较需要已验证的分组汇总算法或后端聚合 |
| 待上架批次 | `库存状态=待上架` 已有，可按状态计数；但“最早等待 6 小时”没有入库时间或状态变更时间 | 确认是否合计原料、包材、成品三张表的待上架批次；等待时长需要增加 `入库时间@(Type=DateTime)` 或进入待上架状态的时间 | 单表状态计数可用 `(If(库存状态=待上架))&VecDimCount(...)`；三表合计与等待时长需要跨表汇总和时间差算法或后端聚合 |
| 今日出入库 | 入出库记录的 `操作时间` 是 `09-01 09:25` 形式的展示字符串，不是可比较的日期时间字段 | 将操作时间改为 `@(Type=DateTime)`；确认统计的是单据条数还是数量、是否只计已过账单据，以及入库和出库分别对应哪些单据类型 | 需要日期过滤加按单据类型分组计数；当前模型未证明该组合可运行，需新增运行时日期算法或后端聚合 |

动态化实施前还需确认：统计是否随当前 Tab 或 Table 筛选联动、单据过账后何时刷新、库存总览与三类库存表之间是否会重复计数，以及由 ILD 运行时还是后端聚合接口负责计算。在这些条件与算法未验证前，当前模型继续使用固定值。

### 5.3 订单管理

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| O-01 | 已实现 | 销售订单字段、客户/产品/人员引用及网页版样例数据 | 审核规则、后端保存和客户权限需应用实现 |
| O-02 | 部分实现 | 销售与库存订单编号字段及样例 | 自动编号、并发唯一和历史编号保护需编号服务 |
| O-03 | 部分实现 | 订单产品引用和产品展示值 | 特性组合匹配、工艺/配方带出及审核快照未实现 |
| O-04 | 已实现 | 包装编号、纸盒、纸箱、每盒/每箱数量、喷码及状态 | 专用包材校验和包装版本锁定需应用实现 |
| O-05 | 部分实现 | 订单状态枚举 | 合法状态机、审核和状态留痕未实现 |
| O-06 | 部分实现 | 终止记录、原因、资源释放说明、申请人和状态 | 终止权限与真实资源释放事务未实现 |
| O-07 | 部分实现 | 销售、库存、包装和终止 Table 页面 | 服务端查询、真实进度联查、交互式链路追溯和导出权限未实现 |

#### 订单管理顶部统计卡片

当前“销售订单”页面顶部已增加 `Type=Form,SubType=Block` 的本月订单、待审核、生产中、临近交期四项统计，并按网页版截图暂时固定显示为 `32 / 3 / 12 / 2`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出。

后续改为动态统计前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 本月订单 | 销售订单和库存订单均没有创建时间或下单日期 | 分别增加 `创建时间@(Type=DateTime)` 或 `订单日期@(Type=Date)`；确认是否合计销售与库存订单、是否排除已终止订单，并统一自然月和业务时区口径 | 单表行数可使用 `VecDimCount`；跨两张表合计还需要已验证的加法/汇总表达式。本月范围过滤需要日期算法或后端聚合 |
| 待审核 | 销售订单有待审核状态，库存订单没有待审核状态；终止记录也有待审核状态 | 确认统计范围是仅销售订单，还是同时包含终止申请等审核事项；若显示“最早等待时长”，还需申请时间或进入待审核状态的时间 | 单表状态计数可用 `(If(状态=待审核))&VecDimCount(...)`；跨表审核事项需多表汇总算法或后端聚合 |
| 生产中 | 两类订单都有生产中状态，但没有与生产任务进行唯一、完整的运行时关联 | 确认按订单状态直接计数，还是按存在执行中生产任务的去重订单数；若需显示涉及产线数，还要稳定的订单—任务—产线关联 | 按单表状态可用 `If + VecDimCount`；跨销售/库存两表合计及按任务去重，需要已验证的集合汇总算法或后端聚合 |
| 临近交期 | 已有销售订单 `交期` 和库存订单 `期望完成`，但没有“当前日期至未来 3 天”的已验证过滤表达式 | 确认时间窗口是否包含当天、是否按自然日或工作日、是否排除已完成/已终止、逾期订单是否另计 | 需要当前时间、日期范围比较、状态组合过滤以及跨两表汇总；当前 101 模型未证明该组合可运行，需新增运行时日期统计算法或由后端计算 |

动态化实施前还需确认：四项统计是否随当前 Tab 或 Table 筛选条件联动、状态变更后何时刷新、销售与库存订单的合计是否需要去重，以及统计结果由 ILD 运行时计算还是后端聚合接口提供。在这些条件与算法未验证前，当前模型继续使用固定值。

### 5.4 人工排产

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| S-01 | 部分实现 | 待排订单、排产计划、产线分配和排产明细数据 | 产线适配、齐套校验、剩余量计算和甘特拖拽未实现 |
| S-02 | 部分实现 | 一张订单拆分至多条产线及任务编号样例 | 批量原子提交、幂等下发、库存占用和失败回滚未实现 |

#### 人工排产顶部统计卡片

当前“待排订单”页面顶部已增加 `Type=Form,SubType=Block` 的待排订单、今日计划、已下达、齐套异常四项统计，并按网页版截图暂时固定显示为 `5 / 3 / 14 / 1`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出。

原“排产甘特图”内容目前只是保存甘特条目数据的普通 Table，并没有甘特泳道、时间轴或拖拽能力，因此本轮将该 Tab 改名为“排产明细”。字段仍保留计划开始、计划结束、计划数量、进度和状态，后续若平台提供经过验证的甘特渲染器，可在不丢失数据的前提下再升级展示方式。

后续将顶部统计改为动态计算前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 待排订单 | 当前表同时包含待排产、齐套异常、待审核和已完成记录 | 明确是统计全部入口记录，还是仅统计 `剩余待排 > 0` 且可进入排产的订单；数量副标题还需统一可计算的数值和计量单位 | 单一状态可用 `If + VecDimCount`；多状态、剩余数量比较及数量求和需要已验证的组合过滤与聚合算法 |
| 今日计划 | `计划周期` 和 `执行区间` 当前是展示字符串，不是独立起止日期时间字段 | 增加计划开始、计划结束或计划日期字段；确认按当天开始、当天覆盖还是当天新建统计，以及“覆盖产线数”的去重口径 | 需要当前日期、日期范围判断和产线去重计数；当前参考模型未证明这些组合可运行，需新增运行时算法或后端聚合 |
| 已下达 | 排产计划状态、产线分配状态和是否已生成生产任务是三个不同口径 | 确认按排产计划 `状态=已下达`、产线分配 `状态=已下发`，还是按有效生产任务数量统计；“任务生成正常”还需任务生成结果字段 | 单表状态计数可用 `If + VecDimCount`；跨排产计划、分配和生产任务的一致性统计需要跨表关联算法或后端聚合 |
| 齐套异常 | 当前既有 `状态=齐套异常`，也有自由文本形式的齐套情况 | 明确以状态为准还是根据原料、包材缺口计算；若需展示异常类型，应增加结构化的原料齐套、包材齐套和缺口数量字段 | 状态口径可用 `(If(状态=齐套异常))&VecDimCount(...)`；按物料库存实时判断需要齐套计算、库存聚合和刷新机制，通常应由后端服务完成 |

动态化实施前还需确认：统计是否随 Table 筛选联动、计划与任务状态变更后何时刷新、跨表结果如何去重，以及由 ILD 运行时还是后端聚合接口负责计算。在这些条件与算法未验证前，当前模型继续使用固定值。

### 5.5 生产执行

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| PR-01 | 已实现 | 产线编号、名称、车间、当前订单、当前产品、产出和状态 | 当前值实时同步需生产执行服务 |
| PR-02 | 部分实现 | 任务、计划量、累计报工、完成率、预计结束、责任人和状态 | 状态机、完成率及预计结束自动计算未实现 |
| PR-03 | 已实现 | “生产组织”页签内组织树加产线运营表，树在上、表在下，点树节点筛选表数据 | 树节点上的层级汇总文案（在制订单、计划数、报工数）及组织权限未实现 |
| PR-04 | 部分实现 | 排产、分配、任务、订单和产线编号链路 | 事务化下发、幂等与库存占用未实现 |
| PR-05 | 部分实现 | 成品入库单、批次、数量、库位和状态 | 审核后库存事务未实现 |
| PR-06 | 部分实现 | 任务、物料、批次、数量、库位、操作人和状态 | 自动扣减、防超耗、退料冲销和库存回补未实现 |
| PR-07 | 部分实现 | 报工总数、批次、时间、人员和审核状态 | 累计上限、审核及订单进度联动未实现 |

#### 生产执行页面合并与顶部统计卡片

本轮将原“生产任务”“生产报工”“原料耗用”“成品入库”“生产组织”五个页面合并到同一个“生产任务”页面，并统一改为 `@(Tab=1)` 页签，与网页版生产执行模块的一条页签栏一致。合并只改变页面归属和页签形式，字段、枚举、引用和样例数据均未变动；`$生产执行:生产任务*生产任务` 这一被报工、耗用和入库引用的来源路径保持不变，因此现有 `生产任务号` 引用不受影响。

页面顶部同时增加 `Type=Form,SubType=Block` 的运行产线、执行中任务、今日报工、异常任务四项统计，并按网页版截图暂时固定显示为 `8 / 11`、`12`、`82,460`、`2`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出；`8 / 11` 中的空格会被解析器按第 4 节的归一化规则去除。网页版统计卡片下方的“综合运行率 72.7%”“今日新增 3 个”“计划达成 76.4%”“1 暂停 · 1 超期”等副标题，当前 Form 结构没有已验证的副标题字段，本轮未生成。

后续将顶部统计改为动态计算前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 运行产线 | 产线运营表只有 5 条样例产线，`基础数据:产线档案` 才是全量产线来源，两者口径不一致；分母 11 在模型中没有对应数据 | 明确分子按产线运营 `状态=运行` 还是按产线档案 `状态=运行`，分母按产线档案全量还是按启用产线；“综合运行率”还需确认是按条数还是按运行时长计算 | 单表状态计数可用 `(If(状态=运行))&VecDimCount(...)`；分子分母来自两张表的比值和百分比展示需要已验证的跨表比值与格式化算法 |
| 执行中任务 | 生产任务表同时包含待执行、执行中、已完成和暂停记录，且没有任务创建时间 | 确认是否只统计 `状态=执行中`，是否计入暂停任务；“今日新增”还需增加 `创建时间@(Type=DateTime)` 或下发时间字段 | 状态计数可用 `If + VecDimCount`；“今日新增”需要当前日期与日期范围判断算法或后端聚合 |
| 今日报工 | 生产报工的 `报工时间` 是 `09-01 09:20` 形式的展示字符串，不是可比较的日期时间字段；`报工总数` 带千分位，是文本不是数值 | 将报工时间改为 `@(Type=DateTime)`，并明确报工总数使用无千分位的数值字段；确认是否只统计已审核报工、是否按自然日和业务时区 | 需要日期过滤加数值求和；当前模型未证明该组合可运行，通常应由后端聚合，或先补充可计算的数值字段 |
| 异常任务 | 网页版的“异常”同时包含暂停和超期两类，模型中只有 `状态=暂停`，没有超期判定依据 | 确认异常口径是否等于“暂停 + 超期”；超期需要可比较的 `预计结束@(Type=DateTime)` 字段和逾期规则，当前该字段为 `09-03 16:20`、`待重排` 等混合文本 | 暂停计数可用 `(If(状态=暂停))&VecDimCount(...)`；超期需要当前时间与计划结束时间比较，并处理非日期文本，需新增运行时日期算法或后端计算 |

动态化实施前还需确认：统计是否随当前 Tab 或 Table 筛选联动、报工与入库过账后何时刷新、跨生产任务与产线运营的结果如何去重，以及由 ILD 运行时还是后端聚合接口负责计算。在这些条件与算法未验证前，当前模型继续使用固定值。

#### “生产组织”页签的树加表展示（已实现）

网页版生产执行的第五个页签在同一个页签内自上而下显示两块内容：上方的工厂—车间—产线组织树，下方受树节点筛选的产线运营表。

> 本节此前写作“左侧的组织树，以及右侧的产线运营表（点击树节点筛选下方产线运营数据）”，前半的“左侧/右侧”有误。网页示例 `src/components/ModuleView.vue` 中 `<OrgTree>` 与 `<div class="business-table-wrap">` 是普通文档流中的兄弟节点，树在上、表在下；`src/data/modules/production.ts` 的该页签也注明“点击组织树节点可筛选下方产线运营数据”。以“上下”为准。

平台已支持该能力，本轮据此实现：

- `@Tab` 由标量扩展为元组 `(标识, 页签名)`。同一标识、同一页签名的多个组合并进同一个页签，页签标题取页签名。旧写法 `@(Tab=1)` 语义不变，模型内其余 26 处均未改动。
- 页签内**默认上下堆叠**，成员按声明顺序自上而下、各占整宽；需要左右并排时才另写 `@Size=(宽比,高比)`。本页签不写 `@Size`。
- 新增 `@FilterBy=(源组名, 本组字段)` 声明联动。本页签写 `FilterBy=(生产组织树,产线名称)`。

树节点选中的筛选口径为**子树归集**：取所选节点及其全部后代的名称组成集合，表按声明字段匹配该集合。因此工厂、车间、产线三层共用“产线名称”一个字段即可覆盖——

| 选中节点 | 归集集合 | 表中命中 |
| --- | --- | --- |
| 浸渍线 01 | {浸渍线 01} | 1 行 |
| 一车间（浸渍） | {一车间（浸渍）, 浸渍线 01, 浸渍线 02} | 2 行（车间名匹配不到产线名，无害） |
| 唯律制造工厂（根节点） | 空集（特判为取消筛选） | 5 行，即不筛 |

再次点击当前已选中的节点也视为取消筛选。筛选只作用于视图层，不改动表的源数据。

`$基础数据:组织架构*生产组织树` 仍保留一份同样的树结构作为基础档案，节点与网页版 `productionTree` 一致。

**仍未实现**：树节点上的汇总文案（在制订单、计划数、报工数）。这三个数在网页版是静态样例值，模型中既没有对应字段，也没有可用的聚合算法，需要单独确认口径后另行实现。

### 5.6 设备管理

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| EQ-01 | 已实现 | 工厂—车间—产线—设备 Tree、节点汇总和设备台账 | Tree 节点增改删及台账同步依赖运行界面与后端 |
| EQ-02 | 部分实现 | 设备与产线编号及引用 | 数据库唯一约束与正式编号规则未实现 |
| EQ-03 | 已实现 | 网页版设备类别枚举和数据 | 类别维护、扩展和停用规则需应用实现 |
| EQ-04 | 部分实现 | 台账字段、人员/产线引用及样例数据 | 后端保存、Excel 导入和历史保留未实现 |
| EQ-05 | 部分实现 | 台账、实时状态、报警和采集数据页面 | 时间范围查询、运行统计及交互式详情未实现 |
| EQ-06 | 部分实现 | 运行、报警、维护、停用和数据新鲜度 | 中控实时同步、状态区间及断线识别未实现 |
| EQ-07 | 部分实现 | 点位、当前值、范围、单位、频率和采集状态 | 实时折线、标准范围带、超限标记和模拟推送非当前 ILD 能力 |
| EQ-08 | 部分实现 | 可维护的设备 Table 数据结构 | CSV/Excel 模板、逐行校验、部分成功导入和权限未由模型定义 |

### 5.7 基础数据与接口

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| B-01 | 部分实现 | 公共主档、枚举和引用源 | 独立动态字典维护、停用保护和全局刷新未实现 |
| B-02 | 已实现 | 网页版出现的人员样例及人员引用 | 工号、账号绑定、组织权限和正式人员字段需业务确认 |
| B-03 | 部分实现 | 基础数据 Table 页面 | Excel 模板、格式/重复/引用校验、错误行反馈和权限未实现 |
| I-01 | 部分实现 | 接口清单、方向、方式、频率、责任人、最近通信和状态 | 认证、签名、幂等、重试、告警和数据落库未实现 |
| I-02 | 部分实现 | 点位映射、数据类型、单位、频率、接收日志和异常日志，合并为同一页面的 4 个 Tab | 最终字段、错误码、通信协议和现场联调未实现 |

#### 接口中心页面合并与顶部统计卡片

本轮将原“接口清单”“点位映射”“接收日志”“异常日志”四个页面合并到同一个“接口清单”页面，并统一改为 `@(Tab=1)` 页签，与网页版接口中心模块的一条页签栏一致，页签数为 4。合并只改变页面归属和页签形式，字段、枚举、引用和样例数据均未变动；接收日志和异常日志引用的来源路径 `$接口中心:接口清单*接口清单:{接口编码}` 保持不变，现有 `所属接口` 引用不受影响。

页面顶部同时增加 `Type=Form,SubType=Block` 的接口总数、今日接收、重试报文、未处理异常四项统计，并按网页版截图暂时固定显示为 `11`、`28.6 万`、`16`、`1`。这四个值是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出；`28.6 万` 中的空格会被解析器按第 4 节的归一化规则去除。网页版统计卡片下方的“启用 10 · 停用 1”“成功率 99.97%”“均已自动恢复”“时间戳偏差”等副标题，当前 Form 结构没有已验证的副标题字段，本轮未生成。

后续将顶部统计改为动态计算前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 接口总数 | 接口清单当前只有网页版样例行，数量与网页版统计的 11 条不一定一致；启用与停用的拆分依赖状态字段口径 | 确认统计的是全部接口还是仅启用接口，以及是否把 `ERP → MES` 等不同方向的接口都计入同一总数 | 单表行数可用 `VecDimCount`，按状态拆分可用 `If + VecDimCount`；这一项是四项中最接近可直接动态化的 |
| 今日接收 | 接收日志只有样例报文行，没有报文条数或数据点数的数值字段；`接收时间` 是展示字符串，不是可比较的日期时间字段 | 明确“28.6 万”统计的是报文条数、数据点数还是字节量并增加对应数值字段；接收时间改为 `@(Type=DateTime)`；成功率还需要成功与失败报文的判定字段 | 需要日期过滤加数值求和，并按成功/失败分组求比值；报文级明细通常量级很大，实际应由后端聚合而不是 ILD 运行时逐行计算 |
| 重试报文 | 模型中没有重试次数、重试状态或自动恢复结果字段 | 在接收日志或异常日志中增加重试次数、重试结果字段；确认统计的是发生过重试的报文数还是重试动作次数，以及统计时间窗口 | 有了字段后单表条件计数可用 `If + VecDimCount`；重试与自动恢复本身属于第 8 节列出的接口运行能力，不由模型实现 |
| 未处理异常 | 异常日志有状态字段，但“未处理”的判定口径与网页版的处理中、已恢复等状态需要对齐 | 明确哪些状态计入未处理、是否包含处理中，以及是否限定时间窗口 | 单表状态计数可用 `(If(状态=...))&VecDimCount(...)`；如需按异常类型分组展示还需要结构化的错误码分类字段 |

动态化实施前还需确认：统计是否随当前 Tab 或 Table 筛选联动、报文接收与异常恢复后何时刷新、接收日志的量级是否适合在前端逐行统计，以及由 ILD 运行时还是后端聚合接口负责计算。在这些条件与算法未验证前，当前模型继续使用固定值。

### 5.8 首页

| 编号 | 模型状态 | 已生成内容 | 尚缺内容或边界 |
| --- | --- | --- | --- |
| H-01 | 已实现 | 标题栏系统名称（单字段 `Type=Form,SubType=Text`） | 网页版的日期、问候语和“今日 8 条产线运行”动态文案未生成 |
| H-02 | 部分实现 | 标题下方今日计划产量、今日已完成、在制订单、设备综合效率四个 Block 卡片 | 副标题、进度条和动态计算未实现，见下 |
| H-03 | 部分实现 | 业务链路、待办预警、产线进度、产线进度图和通知列表 | 节点点击跳转、链路自动扫描和实时刷新不属于 ILD 声明能力 |

#### 首页四个核心指标卡片

按网页版截图，首页标题下方是四个并排的统计卡片。本轮把原“关键指标”Table 改为四个 `Type=Form,SubType=Block` 表单组，写法与成熟参考模型 `weilumodel-new.ild` 的首页概览卡片一致：每个卡片是一个只含单字段的组，组名用 `@(ShowHide=Hide)` 隐藏，卡片标题取字段名，卡片数值取该字段唯一的数据值，并用 `Position=(2,1)` 至 `Position=(2,4)`、`Size=(0.25,0.13)` 在同一行并排布局。

这里没有沿用各业务模块顶部“一个 Form 组带四个字段”的写法，原因是首页需要四张彼此独立、可单独定位的卡片，而成熟模型中被验证过的首页卡片形态就是单字段组；`SubType=Text` 的字面量分支在解析器中也只对单字段成立。

四个卡片的值按网页版截图暂时固定为 `108,000 只`、`82,460 只`、`12 单`、`91.8 %`，是第一阶段的界面占位数据，不是根据当前 ILD 表数据计算得出；数值中的空格会被解析器按第 4 节的归一化规则去除。

原“关键指标”Table 中的下列内容本轮没有对应载体：

| 原列 | 原内容 | 当前状态 |
| --- | --- | --- |
| 单位 | 只、只、单、% | 已并入卡片数值文本，不再是独立字段 |
| 变化 / 说明 | 较昨日 +8.2%、计划达成 76.4%、2 单临近交期、较昨日 +1.6% | 当前 Block 表单没有已验证的副标题字段，本轮未生成 |
| 进度 | 76%、76%、58%、92% | 卡片内的进度条不属于已验证的 Form 渲染能力，本轮未生成 |

后续将四个卡片改为动态计算前，需要补齐并确认以下数据与口径：

| 统计项 | 当前缺口 | 动态计算所需数据或规则 | 可复用或待补算法 |
| --- | --- | --- | --- |
| 今日计划产量 | 生产任务的 `计划数量` 带千分位、是文本不是数值，也没有计划日期字段 | 计划数量改为无单位的数值字段并增加计划开始或计划日期；确认是按当天开始的任务、当天覆盖的任务还是按排产计划统计 | 需要日期过滤加数值求和；当前模型未证明该组合可运行，需新增运行时日期算法或后端聚合 |
| 今日已完成 | 与生产执行“今日报工”同源，报工时间是展示字符串、报工总数带千分位 | 报工时间改为 `@(Type=DateTime)`、报工总数改为数值；确认是否只统计已审核报工 | 同生产执行“今日报工”，两处应统一口径，避免首页与模块页数值不一致 |
| 在制订单 | 销售订单与库存订单都有生产中状态，但与生产任务没有唯一运行时关联 | 确认按订单状态直接计数，还是按存在执行中生产任务的去重订单数；“2 单临近交期”还需交期比较规则 | 单表状态计数可用 `If + VecDimCount`；跨两类订单合计与去重需要已验证的集合汇总算法或后端聚合 |
| 设备综合效率 | 模型中没有 OEE 及其可用率、性能、良率三个因子的任何字段 | 需要设备运行时长、计划时长、理论节拍、实际产出和良品数等采集字段，并确认 OEE 的计算口径与统计周期 | OEE 属于跨设备、跨时间的聚合计算，应由后端或设备服务提供，不适合在 ILD 运行时逐行计算 |

动态化实施前还需确认：四个卡片是否随页面刷新联动、与生产执行模块同名统计如何保持一致，以及由 ILD 运行时还是后端聚合接口负责计算。在这些条件与算法未验证前，当前模型继续使用固定值。

### 5.9 记录详情页

网页版每张表点“订单编号 / 生产任务号 / 库存记录号”会打开记录详情抽屉：最上是业务主键和状态，中间是该行全部字段，下面是“关联与追溯”说明。此前整个 ILD 模型只有列表页，没有任何可进入单条记录的页面，本轮补齐。

#### 写法对齐 101 模型的 设备管理:试验设备

表格主键列之所以能点进去，靠的是在**同一个页面路径**下再声明一批详情组：

```ild
$设备管理:{试验设备}@(ShowHide=Hide)*设备概要:{系统编号,设备名称,出厂编号,...}@(EditRead=Read,Type=Form,SubType=Text,MainParam=(系统编号))
```

路径里的 `{列表页}` 段表示“该列表页里被点中的那一条记录”，构造出的页面路径是 `菜单/列表页/`（末尾带斜杠），**不产生新的菜单项**；主表 `MainParam` 指定的那一列因此成为链接列，点一下带着该行进入详情。本模型按同一形状生成：

```ild
$订单管理:{销售订单}@(ShowHide=Hide,InputRequired=Yes,Placeholder=请选择订单编号,EmptyText=请先选择订单编号)*订单基本信息:{订单编号,客户,产品,...}@(EditRead=Read,MaxColumn=8,MaxRow=8,Type=Form,SubType=Text,TextSize=16,MainParam=(订单编号))
```

要点：

- 详情组的字段**直接用主表的同名字段**，不写 `==` 引用，也不为每条记录写实例数据。平台进入详情页时把点中的行作为 `extra.row` 传入，`calculator/common/page.ts::getDetailRowFieldValue` 按 `prop` 逐个取值填进 Form，找不到时才退回声明里的默认值。
- 每个详情组都带 `MainParam=(主键)`，与主表一致；状态、优先级、类别等枚举连同颜色一并重复声明，详情页里的状态色块与列表一致。
- `@(ShowHide=Hide)` 让详情页不出现在菜单里，`InputRequired/Placeholder/EmptyText` 控制未选记录时的空态文案（`calculator/common/contextEmptyState.ts`）。
- 101 模型在模板之外还写了 `$设备管理:试验设备:[主键1,主键2,...]@(ShowHide=Hide)*设备概要:{...}:[...]` 这类实例数据。本模型没有写，因为详情字段与主表同名，值由点中的行填入；若目标平台上出现空值，按同一写法补实例数据即可。

#### 一次走错的路（已删除）

第一版把详情页写成 `$菜单:{记录页}$详情页*组`，并为每个模块另建了一个隐藏的“记录页”存放 `Type=Auto` 整组引用的主表。那是 101 模型里“试验任务 / 试验准备”那种**子页面**写法：它构造出的是 `菜单/详情页` 这样的新页面路径，只能从面包屑下拉进入，主键列仍然点不动，而且新增的两个页面照样出现在左侧菜单里。相关声明本轮已全部删除，不再有隐藏记录页。

#### 已生成的详情页

| 模块 | 详情上下文路径 | 链接列（主键） | 详情组 |
| --- | --- | --- | --- |
| 订单管理 | 订单管理/销售订单/ | 销售订单 · 订单编号 | 订单基本信息、关联与追溯 |
| 人工排产 | 人工排产/待排订单/ | 待排订单 · 订单编号 | 排产基本信息、关联与追溯 |
| 生产执行 | 生产执行/生产任务/ | 生产任务 · 生产任务号 | 任务基本信息、关联与追溯 |
| 仓库管理 | 仓库管理/库存总览/ | 库存总览 · 库存记录号 | 库存基本信息、关联与追溯 |
| 产品库 | 产品库/产品档案/ | 产品档案 · 产品编号 | 产品基本信息、关联与追溯 |
| 设备管理 | 设备管理/设备台账/ | 设备台账 · 设备编号 | 设备基本信息、关联与追溯 |
| 接口中心 | 接口中心/接口清单/ | 接口清单 · 接口编码 | 接口基本信息、关联与追溯 |

`XX基本信息` 是 `Type=Form,SubType=Text` 的字段清单，对应网页版抽屉的字段区；`关联与追溯` 是 `EditRead=Read` 的只读文本，字段不在主表中，取声明里的固定文案，对应抽屉底部那段链路说明。7 个详情页共 59 个字段，已逐个核对与主表同名。

页面布局不受影响：详情页挂在 `菜单/列表页/` 下，各业务页的顶部统计卡片、页签和表格声明一行未动。101 模型的 `设备管理/试验设备` 同样是三个统计 Form 在前、`设备列表` 在后，详情照常工作。

#### 状态与边界

| 内容 | 模型状态 | 说明 |
| --- | --- | --- |
| 主键列点击进入详情 | 已实现 | 路径构造为 `菜单/列表页/`，与 101 模型 `设备管理/试验设备/` 形状一致 |
| 详情字段清单与状态色 | 已实现 | 59 个字段与主表同名同序，枚举与颜色一并声明 |
| 点中行的值填充 | 部分实现 | 依赖 `getDetailRowFieldValue` 按 prop 取值，本轮未在浏览器验证；必要时按 101 写法补实例数据 |
| 详情页内编辑回写 | 尚未实现 | 当前详情组为 `EditRead=Read`，与 101 模型 `设备概要` 一致；网页版抽屉的“✎ 编辑”未映射 |
| 按当前记录过滤的关联子表 | 尚未实现 | 需要把当前上下文写进 `If(...)` 条件（101 模型中该写法用于 `EditRead` 与 `GenerateOperationPage`，用于过滤数据行本轮未验证），不虚构 |
| 网页版“查看完整业务链路”弹窗 | 模型能力尚不能实现 | 见第 8 节第 9 条 |
| 其余页签的详情（库存订单、包装信息、终止记录、排产计划、生产报工等） | 尚未实现 | 每个模块本轮只按页面同名主表生成一个详情；其余页签按同一写法扩展即可 |

## 6. 跨模块链路

| 编号 | 模型状态 | 当前模型可表达 | 尚缺内容 |
| --- | --- | --- | --- |
| F-01 | 部分实现 | 订单、产品、排产、分配、任务、耗用、报工、入库、库存的编号与字段链；7 个模块已有单条记录详情页作为链路入口（见 5.9） | 网页版自动扫描并绘制链路的弹窗交互不属于 ILD 声明能力 |
| F-02 | 部分实现 | 库存订单、排产、任务和成品库存样例链 | 库存校验、自动联动和事务一致性未实现 |
| F-03 | 部分实现 | 任务、批次、耗用、入库、出入库和库存记录可按编号核对 | 双向自动追溯图、实时库存和进度计算未实现 |
| F-04 | 部分实现 | 设备、点位、报警、采集数据、接口和报文编号关联 | 中控真实数据、实时状态和异常恢复未实现 |

## 7. 尚未实现但可继续建模

以下内容不是解析器绝对不能表达，而是当前网页版缺少确定的数据或本轮没有足够规则，暂不虚构：

1. P-06 换产成本项目、工时、金额和比较页。
2. 更完整的数据字典分类、字典值排序、停用影响范围和编号规则页面。
3. 客户、人员、物料、仓库、库区和产线的正式业务字段。
4. 有明确平台函数实现后可增加的统计计算或生成按钮。
5. 经确认后的独立追溯查询结果表或报表数据载体。

## 8. 模型能力尚不能实现

以下能力不能仅靠当前 ILD 文本可靠完成：

1. 登录、角色权限、字段权限、数据范围和审批流。
2. 服务端自动编号、并发唯一约束、编号锁定和规则版本。
3. 数据库事务、库存占用、释放、过账、冲销、回滚和防超量。
4. FIFO 自动选批、异常跳批、批次锁定和人工例外审批。
5. 订单、排产、任务、报工、入库等状态机和非法流转拦截。
6. 产品多特性匹配、工艺/配方自动带出及历史版本快照。
7. 网页版甘特条拖拽改期和产线泳道渲染。
8. 设备实时折线、3 秒模拟推送、标准范围带和动态超限标记。
9. 网页版跨模块自动扫描、排序、跳转和高亮的追溯弹窗（单条记录的详情页本身已在 5.9 生成，不能实现的是自动扫描并绘制链路的弹窗）。
10. CSV/Excel 模板下载、表头/必填/重复校验、部分成功导入和错误行反馈。
11. 批量选择、批量状态更新、二次确认删除和浏览器本地持久化重置。
12. 通知已读状态、快捷键、Toast 提示和用户菜单交互。
13. 中控接口认证、报文校验、实时通信、重试、告警和数据落库。
14. 审计日志、历史不可篡改、正式删除限制及导出审计。

## 9. 验证状态

本轮补记录详情页后重新核对，验证层次与结论如下。

- 文本静态检查：普通括号、字段括号、数据括号、中文方括号和块注释已检查平衡，新增声明未出现不平衡。
- 真实文本分行器：直接引入仓库中的 `calculator/common/dealString.ts::analyzerStringToArray`（该模块无 import，可用 `node --experimental-strip-types` 直接执行），按 `struct` 相同的参数 `[',', ':', '@', '=', '&', '*']` 对模型文本执行。结果：补详情页前 57 条活动声明，补后 71 条，全部以 `$` 开头，新增的块注释没有被并进任何声明。
- 真实路径解析：按 `calculator/model/main.ts::analyzeStringToKeyAndValue` 与 `calculator/model/index.ts::analyzeMenu` 的逻辑，配合真实的 `analyzeSplitToContent` 和 `menuPathAnnotations.ts::extractMenuPathAnnotations` 计算菜单路径。结果：21 个原有页面路径全部不变，另外解析出 7 个末尾带斜杠的隐藏详情上下文 `订单管理/销售订单/`、`人工排产/待排订单/`、`生产执行/生产任务/`、`仓库管理/库存总览/`、`产品库/产品档案/`、`设备管理/设备台账/`、`接口中心/接口清单/`，每个 2 个组。用同一套代码解析 `new-TPC-R101model(2.1)(24)test.ild`，其 `设备管理/试验设备` 为显示、4 个组，`设备管理/试验设备/` 为隐藏、8 个组，与本模型形状一致。
- 字段与数据：7 个详情组共 59 个字段，逐个核对均能在对应主表的字段列表中按同名找到；7 个 `MainParam` 与主表声明一致。
- 引用验证：原有 64 个引用字段的来源页面、来源组和来源字段均存在；422 个已有引用值全部命中来源值。详情组不新增引用，也不新增业务数据。
- 网页数据对照：覆盖 32 个页签、214 行、1839 个业务单元格；按解析器空格和逗号归一化规则进行语义比较。
- 结构构造：本轮未执行完整的 `struct`（它依赖 pinia、lodash-es、DOM 和接口，无法在当前工作区独立运行），因此详情组的 Form 结构对象尚未经过真实构造验证。
- 浏览器运行：尚未在真实模型导入页确认。详情页需要重点确认三件事：列表里主键列是否渲染为链接列并可点击、点进去后字段是否按点中行填值（`getDetailRowFieldValue` 按 prop 取值）、未选记录时是否显示 `EmptyText` 空态；以及原有页签、顶部统计 Form 和首页 Block 卡片是否未受影响。
- 持久化及往返：尚未连接平台后端执行保存、导出、重导入。详情页用到的动态路径 `{列表页}` 段和 `MainParam=(...)` 等带括号的过滤器存在导出时中文逗号回写的已知风险（见规则第 10 节），往返前不能视为已通过。

因此本次记录详情页完成到“文本解析与路径构造”层：分行、声明边界、菜单路径、隐藏标记、字段/主键对齐均已用仓库中的真实解析代码复核，并与 101 模型的同类页面做了形状比对；结构构造、主键列点击、值填充、持久化和 ILD 导出往返仍需在目标平台中验收。
